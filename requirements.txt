# FastAPI and web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Pydantic for validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client
httpx>=0.27.0

# Redis for shared task storage
redis>=5.0.0

# Logging
logzero>=1.7.0

# Docling - document conversion library
# https://github.com/docling-project/docling
docling>=2.12.0

# PDF processing
pypdfium2>=4.30.0

# GPU acceleration (optional but recommended)
torch>=2.0.0

# Image processing (used by Docling OCR)
Pillow>=10.0.0

# Additional dependencies for Docling features
# These are typically installed automatically with docling
# but listed explicitly for clarity
easyocr>=1.7.0  # OCR support
pdf2image>=1.16.0  # PDF to image conversion
python-dotenv>=1.0.0  # Environment variable support
